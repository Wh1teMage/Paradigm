local ReplicatedStorage = game:GetService('ReplicatedStorage')
local HttpService = game:GetService('HttpService')
local Players = game:GetService('Players')

--require(ReplicatedStorage.Components.SignalComponent):GetSignal('Test'):Connect(function(scope, ...)
	--print(scope, ...)
	
	--print(require(ReplicatedStorage.Components.SignalComponent.DataTransfer):Decode(...))
	--local args = {...}
	--local res = buffer.readstring(args[1], 0, args[2])
	
	
	
	--print(res, args[1], buffer.len(args[1]))
--end)


